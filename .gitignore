# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to check in explicitly
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*
*.tfplan

# Ignore CLI configuration files
.terraformrc
terraform.rc
mlflow-cert/*.pem

# macOS
*.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg

# Virtual environments
.env
.venv
venv/
ENV/
env/

# IDEs and editors
.idea/
.vscode/
*.swp
*.swo

# Build artifacts
build/
dist/
*.whl

# MLflow
mlruns/

# Checkpoints (if you don't want to track them by default)
# /checkpts/ # Or more specific patterns

# Logs
*.log

# Secrets - ensure these are not committed
*.key
*.pem # Be careful if some .pem like CA certs ARE meant to be in repo
# specific_secret_file.txt

# Jupyter Notebook
.ipynb_checkpoints